const initialState = 0;

const format1Reducer = (state = initialState, action) => {
    switch (action.type) {
        case 'CALCULATE_SALT':
            {

            }
        default:
            return state;
    }
}

export default format1Reducer;